name: build-and-push

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      push:
        description: 'Push to ghcr.io'
        required: false
        default: 'false'

# Default GITHUB_TOKEN has packages:read but NOT packages:write.
# Must explicitly grant write to allow docker/build-push-action to
# push to ghcr.io/<user>/<repo>. Without this, push fails with:
#   denied: installation not allowed to Create organization package
permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: 929baselineai1/llama-rocmfpx-strix

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: fedora
            tag: fedora-43
          - target: ubuntu
            tag: ubuntu-24-04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v') || github.event.inputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (and push on tag)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Containerfile.${{ matrix.target }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.tag }}
            ${{ matrix.tag == 'fedora-43' && format('{0}/{1}:latest', env.REGISTRY, env.IMAGE_NAME) || '' }}
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v') || github.event.inputs.push == 'true' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
